<Window x:Class="MusicLibrary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:model="clr-namespace:MusicLibrary"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Menu -->
            <RowDefinition Height="*"/>
            <!-- Content -->
        </Grid.RowDefinitions>

        <!-- MENU -->
        <Menu Grid.Row="0">
            <MenuItem Header="_Edit">

                <MenuItem Header="Add">
                    <MenuItem Header="Playlist" Click="AddPlaylist_Click"/>
                    <MenuItem Header="Artist" Click="AddArtist_Click"/>
                    <MenuItem Header="Track" Click="AddTrack_Click"/>
                </MenuItem>

                <MenuItem Header="Update">
                    <MenuItem Header="Playlist" Click="UpdatePlaylist_Click"/>
                    <MenuItem Header="Artist" Click="UpdateArtist_Click"/>
                    <MenuItem Header="Track" Click="UpdateTrack_Click"/>
                </MenuItem>

                <MenuItem Header="Delete">
                    <MenuItem Header="Playlist" Click="DeletePlaylist_Click"/>
                    <MenuItem Header="Artist" Click="DeleteArtist_Click"/>
                    <MenuItem Header="Track" Click="DeleteTrack_Click"/>
                </MenuItem>

            </MenuItem>
        </Menu>

        <!-- CONTENT -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <!-- LEFT PLAYLIST-->

            <Border Grid.Column="0"
                    BorderBrush="LightGray"
                    BorderThickness="1"
                    Margin="5">

                <DockPanel>
                    <TextBlock Text="Playlists"
                               FontWeight="Bold"
                               Padding="5"
                               DockPanel.Dock="Top"/>

                    <ListBox ItemsSource="{Binding Playlists}"
                             SelectedItem="{Binding SelectedPlaylist, Mode=TwoWay}"
                             DisplayMemberPath="Name"
                             Margin="5"/>
                </DockPanel>

            </Border>


            <!-- MIDDLE: ARTISTS -->

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Column="1"
                        BorderBrush="LightGray"
                        BorderThickness="1"
                        Margin="5">     
                    <DockPanel>
                        <TextBlock Text="Artists"
                                 FontWeight="Bold"
                                 Padding="5"
                                 DockPanel.Dock="Top"/>

                        <TreeView x:Name="myTreeView"
                                  SelectedItemChanged="TreeView_SelectedItemChanged" Margin="5">
                            <TreeView.Resources>
                                <HierarchicalDataTemplate DataType="{x:Type model:Artist}" ItemsSource="{Binding Albums}">
                                    <TextBlock>
                                         <Run Text="{Binding Name}" />
                                         <Run Text=" (" /><Run Text="{Binding Albums.Count, Mode=OneWay}" />
                                         <Run Text=" albums)" />        
                                    </TextBlock>
                                </HierarchicalDataTemplate>
                                <HierarchicalDataTemplate DataType="{x:Type model:Album}" ItemsSource="{Binding Tracks}">
                                    <TextBlock Text="{Binding Title}" />
                                </HierarchicalDataTemplate>
                                <HierarchicalDataTemplate DataType="{x:Type model:Track}">
                                    <TextBlock Text="{Binding Name}" />
                                </HierarchicalDataTemplate>
                            </TreeView.Resources>
                        </TreeView>

                    </DockPanel>

                </Border>
            </Grid>

            <!-- RIGHT -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="myDataGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeColumns="False"
                          CanUserResizeRows="False"
                          CanUserReorderColumns="False"
                          CanUserSortColumns="True"
                          SelectionMode="Extended"
                          SelectionUnit="CellOrRowHeader"
                          RowHeaderWidth="28"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          AlternationCount="2"
                          RowBackground="White"
                          AlternatingRowBackground="NavajoWhite"
                          ItemsSource="{Binding Tracks}"
                          ScrollViewer.ScrollChanged="DataGrid_ScrollChanged"
                          IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                          SelectedItem="{Binding SelectedPlaylistTrack, Mode=TwoWay}">

                    <DataGrid.RowHeaderStyle>
                        <Style TargetType="DataGridRowHeader">
                            <Setter Property="Content" Value="🗑"/>
                            <Setter Property="ToolTip" Value="Ta bort låt från playlist"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Foreground" Value="DarkRed"/>
                            <Setter Property="Padding" Value="5"/>
                            <EventSetter Event="PreviewMouseLeftButtonDown"
                                         Handler="RowHeader_DeleteClicked"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="Red"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowHeaderStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Track" Binding="{Binding Name}" FontStyle="Italic" Width="*"/>
                        <DataGridTextColumn Header="Album" Binding="{Binding Album.Title}" Width="*"/>
                        <DataGridTextColumn Header="Length (ms)" Binding="{Binding Milliseconds}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</Window>
