<Window x:Class="MusicLibrary.Views.EditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Width="380">

            <!-- Välj befintlig (Update/Delete) -->
            <StackPanel Margin="0,0,0,10" Visibility="{Binding ShowSelector, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="{Binding SelectorLabel}" Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding SelectorItems}"
                          SelectedItem="{Binding SelectedSelectorItem}"
                          DisplayMemberPath="{Binding SelectorDisplayMember}"
                          />
            </StackPanel>

            <!-- Name (Playlist/Artist/Track) -->
            <StackPanel Margin="0,0,0,10" Visibility="{Binding ShowName, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="{Binding NameLabel}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <!-- Track fields -->
            <StackPanel Visibility="{Binding IsTrack, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Length (ms)" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding MillisecondsText, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!--<StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Media Type" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding MediaTypes}"
                              SelectedItem="{Binding SelectedMediaType}"
                              DisplayMemberPath="Name"/>
                </StackPanel>-->

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Album" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Albums}"
                              SelectedItem="{Binding SelectedAlbum}"
                              DisplayMemberPath="Title"/>
                </StackPanel>

                <!--<StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Genre (optional)" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Genres}"
                              SelectedItem="{Binding SelectedGenre}"
                              DisplayMemberPath="Name"/>
                </StackPanel>-->

                <!--<StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Composer (optional)" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Composer, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>-->
            </StackPanel>

            <TextBlock Text="{Binding ErrorText}" Foreground="DarkRed" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="{Binding ConfirmLabel}" Width="90"
                    Command="{Binding ConfirmCommand}"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>

